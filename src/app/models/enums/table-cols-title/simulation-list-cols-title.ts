import { TableColsTitle } from '../table-cols-title';

export enum SimulationListColsTitle {
  EMPTY,
  NAME = TableColsTitle.NAME,
  CREATION_DATE = TableColsTitle.CREATION_DATE,
  STATUS = TableColsTitle.STATUS,
  ACTIONS = TableColsTitle.ACTIONS,
}
